const user = {
    name: String,
    chatList: {
        name: {
            msg: Number,
            msg: Number,
            msg: Number,
        },
        name: {
            msg: Number,
            msg: Number,
            msg: Number,
            msg: Number,
        },
        name: {
            msg: Number
        },
    }
}